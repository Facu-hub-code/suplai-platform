# Referencia visual (opcional)

Colocá aquí una captura real del distribuidor, por ejemplo `lista-manuscrita.jpg`.

El E2E actual envía `mensaje_simulado.txt` al webhook (equivalente a la salida de visión en producción). Cuando integremos webhook v2 con imagen binaria, este archivo servirá de golden sample.
