# Referencia visual — lista Nutrispa

Pedido fotografiado por WhatsApp. El E2E envía `mensaje_simulado.txt` (OCR simulado) porque el webhook v1 no sube binarios.

Productos esperados en catálogo:
- Sweetcorn frozen 30 lb (`89090009`) — 5 CS
- CUT GREEN BEANS 22 lb (`89090130`) — 2 CS
- Spinach Frozen IQF Box 24lb (`89090038`) — 1 CS
