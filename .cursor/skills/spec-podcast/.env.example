# Copiar a .env y completar (nunca commitear .env).
# cp .env.example .env

# auto = ElevenLabs → OpenAI → macOS (recomendado)
PODCAST_TTS_BACKEND=auto

# ElevenLabs — https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=
# Voz masculina por defecto (Adam). Cambiar si tenés otro voice_id.
PODCAST_VOICE_ID=pNInz6obpgDQGcFmaJgB
ELEVEN_MODEL=eleven_multilingual_v2

# OpenAI — fallback si ElevenLabs falla (créditos, rate limit, red)
OPENAI_API_KEY=
OPENAI_VOICE=onyx
OPENAI_MODEL=gpt-4o-mini-tts

# Último fallback macOS (sin credenciales ni red)
# MACOS_VOICE=Reed
# RATE=170
