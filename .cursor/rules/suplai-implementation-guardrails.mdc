---
description: Guardrails para implementadores no técnicos — onboarding de tenants Suplai
globs: implementacion/**
alwaysApply: false
---

# Implementación Suplai — guardrails

Aplica cuando trabajás en `implementacion/**` o el usuario pide implementar/onboarding de un distribuidor.

## Alcance

- **MUST** trabajar solo en `implementacion/{schema_name}/` del tenant activo.
- **MUST NOT** abrir rama git, worktree ni PR para carga de datos o avances de fase; la carpeta del tenant es el aislamiento (ver `suplai-implementation-no-branch`).
- **MUST** ejecutar Fase 0 (preflight) antes de Fase 1; no saltar fases del pipeline.
- **MUST** pedir `schema_name` al inicio y **repetirlo textualmente** antes del primer `INSERT`/`UPDATE`/`DELETE` en Supabase.
- **MUST** generar el CSV de salida de la fase y pedir al implementador que lo revise **antes** de cargar a BD.
- **MUST** cerrar cada fase actualizando `manifest.yaml` y verificando conteos con MCP (`SELECT`).

## Prohibiciones

- **MUST NOT** modificar schemas distintos al `{schema_name}` confirmado.
- **MUST NOT** alterar `public.distribuidoras` salvo lectura o campos acordados en la skill (ej. metadata de ciudad).
- **MUST NOT** ejecutar Fase 9 (purga) sin la frase exacta del implementador: `PURGE MOCK {schema_name}`.
- **MUST NOT** borrar datos si `COUNT(*) > 0` en productos/clientes y el implementador no confirmó que es entorno de prueba/mock.
- **MUST NOT** pedir al implementador que escriba SQL; vos proponés pasos en lenguaje simple.

## Credenciales del owner

- **MUST** usar email `admin@{schema_name}.com` (o el dominio real del cliente si `{schema}.com` es inválido) y contraseña **`Suplai2026`** al crear el owner.
- **MUST NOT** inventar otra password (`ForsaDemo2026!`, `{Tenant}Demo2026!`, etc.) salvo pedido explícito.

## Modo por defecto — datos finales

- **MUST** tratar `manifest.modo` vacío o `completo` como implementación con datos finales.
- **MUST** cargar todos los SKUs con Precio > 0. `is_mock=false`.
- **MUST NOT** inventar 4 listas de precios, vendedores, clientes, pedidos, chats, tickets ni promociones.
- **MUST** omitir (no simular) las fases 2–8 si no hay origen real. Fase 1.1 es opcional sobre el catálogo ya cargado.
- **MUST NOT** escribir `image_url` placeholder.

## Catálogo en demo agéntica (solo si se pide)

- Activar recorte **solo** si el implementador dice **demo agéntica** o `manifest.modo = demo`.
- **MUST** recortar a **80–100 SKUs** descriptivos del negocio cuando el origen tiene más de 100 ítems con precio.
- **MUST** guardar el universo en `inputs/catalogo-completo.csv` y cargar solo el recorte.
- **MUST NOT** omitir Fase 1.2 en modo demo: se nutren esos 80–100 (descripciones, aliases, UMV local).
- **MUST NOT** armar demos de 20–25 SKUs salvo pedido explícito.

## Vocabulario con el implementador

Usar: "confirmar", "revisar CSV", "siguiente fase", "schema", "cantidad de productos cargados".

Evitar: jerga de migraciones, transacciones, FK, sin explicar en una línea.

## Bloqueos

- Si falta `is_mock` en tablas: seguir con CSV marcado `is_mock=true` y registrar `blocked: is_mock_migration` en manifest; Fase 9 queda bloqueada.
- Si preflight falla (tenant con datos sin acuerdo): **STOP** y escalar.

## Referencia

- Resumen del flujo: `docs/implementacion/flujo-agentico-resumen.md`
- Orquestador: skill `suplai-implementation`
