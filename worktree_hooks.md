# Worktree hooks — ver documentación

Los hooks y scripts de entorno local para worktrees están documentados en:

**[docs/dev/worktree-local-dev.md](docs/dev/worktree-local-dev.md)**

Instalación rápida:

```bash
bash scripts/worktree/install-hooks.sh
bash scripts/worktree/add-worktree.sh backend fix/mi-rama
```
